import React from 'react'

/**
 * 
 * @param onChange Callback used when user press the switch
 * @returns Toggle switch for change a bool value
 */

export default function Switch({onChange}) {
    return (
        <div>
            
        </div>
    )
}
