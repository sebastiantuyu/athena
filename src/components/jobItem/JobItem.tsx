import React, { ReactElement } from 'react'
import JobInterface from './JobInterface'


/**
 * 
 * @param userData {@see JobInterface}
 * @param onClick  Callback function 
 * @returns Clickable card
 */
export default function JobItem(userData: JobInterface, onClick: Function): ReactElement {
    return (
        <div>
             
        </div>
    )
}
